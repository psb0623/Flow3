// @flow
import * as React from 'react';
import {StyleSheet, View} from 'react-native';

type Props = {
  answer: string;
};

export const SharePatternScene = ({answer}: Props) => {
  return <View></View>;
};

const styles = StyleSheet.create({});
